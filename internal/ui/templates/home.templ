package templates

templ Home(v HomeView) {
	@Layout(v.Title) {
		<h1>{ v.Title }</h1>
		if v.Flash != "" {
			<p class="flash" role="status">{ v.Flash }</p>
		}
		<p class="empty">No recipients configured yet.</p>
	}
}
